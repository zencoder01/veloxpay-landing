import InfoPage from "@/components/landing/info-page";

export default function DevelopersPage() {
  return (
    <InfoPage
      title="Developers"
      description="Integrate faster with clear API references, webhooks, sandbox mode, and reliable transaction callbacks."
    />
  );
}
